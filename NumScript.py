# --- NumScript ---

if __name__ == "__main__":  
    # --- Importing NumScript Class from Source ---
    from Source.builder import NumScript

    # --- Launching the NumScript shell ---                
    shell = NumScript()
    shell.cli()